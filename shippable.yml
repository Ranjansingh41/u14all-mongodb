language: none

services:
  - mongodb
  - neo4j
  - rabbitmq
  - redis


build:
  pre_ci_boot:
    image_name: drydock/u14all
    image_tag: master
    pull: true
    
  ci:
  - mongodb -version
  - neo4j -version
  - rabbitmq -version
  - redis -version
    

